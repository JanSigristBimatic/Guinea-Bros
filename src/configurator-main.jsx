import React from 'react';
import ReactDOM from 'react-dom/client';
import GLBConfigurator from './GLBConfigurator';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GLBConfigurator />
  </React.StrictMode>
);
